import 'package:flutter/material.dart';

class QuestsView extends StatelessWidget {
  const QuestsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Quests Content'),
    );
  }
}
